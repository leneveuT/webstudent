{% extends 'base.html.twig' %}

{% block title %}Maisons{% endblock %}

{% block body %}
    <table>
        <tr>
            <th>id</th>
            <th>code</th>
            <th>nom</th>
            <th></th>
        </tr>
        {% for home in homes %}
            <tr>
                <td>{{ home.id }}</td>
                <td>{{ home.code }}</td>
                <td>{{ home.name }}</td>
                <td><a href="#">Afficher</a></td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}
